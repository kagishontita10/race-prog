name: Repository Validation
on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check docs folder
        run: echo "Checking docs folder..."
      - name: List files
        run: ls -R
      - name: Validation passed
        run: echo "Validation passed!"
